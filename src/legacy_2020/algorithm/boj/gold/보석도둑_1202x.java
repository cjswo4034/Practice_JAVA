package algorithm.boj.gold;

// gold 2
public class 보석도둑_1202x {
    // 1) 보석 정보를 최대힙에 저장한다. (n lonN)
    // 2) 가방 무게를 오름차순으로 정렬한다.   (n logN)
    // 3) 보석 무게를 넣을 수 있는 최소한의 가방을 이분탐색으로 찾는다. (logN)
    // 4) 3)에서 가방이 이미 사용중이라면 3)부터 마지막 가방까지 비어있는 최소한의 가방을 이분탐색으로 찾는다. (logN)
}
